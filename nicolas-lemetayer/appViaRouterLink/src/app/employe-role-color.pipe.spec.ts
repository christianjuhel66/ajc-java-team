import { EmployeRoleColorPipe } from './employe-role-color.pipe';

describe('EmployeRoleColorPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeRoleColorPipe();
    expect(pipe).toBeTruthy();
  });
});
