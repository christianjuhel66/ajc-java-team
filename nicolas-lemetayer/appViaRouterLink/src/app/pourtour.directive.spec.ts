import { PourtourDirective } from './pourtour.directive';

describe('PourtourDirective', () => {
  it('should create an instance', () => {
    const directive = new PourtourDirective();
    expect(directive).toBeTruthy();
  });
});
